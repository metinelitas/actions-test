# Action name
name: Test Workflow

on:
    pull_request:

jobs:
    linux-build:
        runs-on: ubuntu-20.04
        steps:
            - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
            - run: echo "🎉 Event ref is ${{ github.event.ref }}."
            - run: echo "🎉 PR payload action ${{ github.event.action }}."
